let bigCode = 50;
console.log(bigCode);

let myName='tolu';
console.log(myName);

function highestNumber(a:number,b:number,c:number,d:number) {
    let number = Math.max(a,b,c,d);
}

function example() {
    let first= prompt('enter first name')
    let second=prompt('enter second name')
    if (first===second) {
        console.log('good');
    }else if (first!==second) {
        console.log('Error');
    }else{
        console.log('something went wrong');
    }
}

function threeNumbers(one:number,two:number,three:number) {
    let numberOne = prompt('enter a number')
    let numbertwo = prompt('enter a number')
    let numberthree = prompt('enter a number')
    let total = Math.max(one,two,three)
}

